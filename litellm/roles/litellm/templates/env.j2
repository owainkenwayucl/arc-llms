LITELLM_MASTER_KEY="sk-{{lookup('ansible.builtin.password', './.master', length=32), chars=['ascii_letters', 'digits'] }}"
LITELLM_SALT_KEY="sk-{{lookup('ansible.builtin.password', './.salt', length=32), chars=['ascii_letters', 'digits'] }}"
